# cs361-file-merging

# simply change the port in the server file to an open port on your machine, then
# send it a string with this syntax "merge!file1name!file2name!newfilename" 
# and it will the merge the first two into the third one